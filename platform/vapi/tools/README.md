GENERATED from `packages/contracts` by `../generate-tools.ts` — do not hand-edit.

The one exception: `transferToHuman.json` will be hand-authored, not generated. It is a Vapi
`transferCall` tool, not a `function` tool — a function tool can only return a string for the model
to speak, it cannot transfer a call (verified against docs.vapi.ai; see Completed/EXECUTED-vapi-n8n-plan §A1 point 5).
