{
  "async": false,
  "function": {
    "description": "Get real services, durations, and prices before you mention ANY of them. Pass the caller's own words as the query. Never state a price or duration this tool did not return in this call; never estimate; never say 'usually about'. If it says the catalogue is unapproved, tell the caller the front desk will confirm the exact price, and offer a callback.",
    "name": "getServicesAndPricing",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "isMember": {
          "default": false,
          "description": "Set true only if lookupCustomer already confirmed membership. Never assume.",
          "type": "boolean"
        },
        "query": {
          "description": "What the caller asked for, in their words, e.g. '60 minute massage' or 'deep tissue'. Pass it through verbatim. If they asked openly what we offer, pass 'services' and you will get the whole list.",
          "maxLength": 120,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "query"
      ],
      "type": "object"
    }
  },
  "messages": [
    {
      "content": "I'm having trouble pulling up our pricing right now.",
      "type": "request-failed"
    }
  ],
  "server": {
    "backoffPlan": {
      "baseDelaySeconds": 1,
      "excludedStatusCodes": [
        400,
        401,
        409,
        422
      ],
      "maxRetries": 1,
      "type": "fixed"
    },
    "credentialId": "${VAPI_TOOLS_CREDENTIAL_ID}",
    "timeoutSeconds": 10,
    "url": "${GRACE_TOOLS_URL}"
  },
  "type": "function"
}
