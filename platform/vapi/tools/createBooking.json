{
  "async": false,
  "function": {
    "description": "Book the chosen slot — only after the caller picked a specific time you offered AND you asked the screening question this call. Pass slotId exactly as checkAvailability gave it, and bookedForName when the appointment is for someone other than the caller. If anything medical came up, do not call this — flagMedicalHold, then hand over.",
    "name": "createBooking",
    "parameters": {
      "additionalProperties": false,
      "properties": {
        "bookedForName": {
          "default": "",
          "description": "First name of the person the appointment is FOR, when it isn't the caller (e.g. booking for a partner). Leave empty when the caller books for themselves.",
          "maxLength": 60,
          "type": "string"
        },
        "contactPreference": {
          "default": "sms",
          "description": "Where they want their confirmation and any payment link. Ask if unsure. Use 'both' only when they actually gave both a number and an email.",
          "enum": [
            "sms",
            "email",
            "both"
          ],
          "type": "string"
        },
        "email": {
          "default": "",
          "description": "Email address, if they gave one. Spell it back before using it. Leave empty if they did not offer one — never invent or assume an address.",
          "maxLength": 254,
          "type": "string"
        },
        "firstName": {
          "description": "Caller first name as they said it. Do not ask for a surname unless they offer one.",
          "maxLength": 60,
          "minLength": 1,
          "type": "string"
        },
        "lastName": {
          "default": "",
          "maxLength": 60,
          "type": "string"
        },
        "medicalScreenPassed": {
          "description": "Set true ONLY after asking the screening question and hearing a clear no. If they said yes, are unsure, or you did not ask, set false and call flagMedicalHold instead of booking.",
          "type": "boolean"
        },
        "notes": {
          "default": "",
          "description": "Scheduling preferences only, e.g. \"prefers a quieter room\". NEVER any health, medical, or diagnostic detail.",
          "maxLength": 280,
          "type": "string"
        },
        "phone": {
          "description": "The caller's mobile number, ONLY as they spoke it aloud on this call. On a web call there is no caller ID, so you must ask. Never use a number from anywhere else and never guess one.",
          "pattern": "^\\+[1-9]\\d{7,14}$",
          "type": "string"
        },
        "phoneConfirmed": {
          "description": "Set true ONLY after you have read the number back digit by digit and the caller confirmed it. If you did not read it back, set false.",
          "type": "boolean"
        },
        "slotId": {
          "description": "Slot id exactly as checkAvailability returned it, e.g. hold-7K2. Never invent one.",
          "pattern": "^[Hh][Oo][Ll][Dd]-[0-9A-Za-z]{3,6}$",
          "type": "string"
        }
      },
      "required": [
        "slotId",
        "firstName",
        "medicalScreenPassed",
        "phone",
        "phoneConfirmed"
      ],
      "type": "object"
    }
  },
  "messages": [
    {
      "blocking": false,
      "content": "Let me get that booked for you.",
      "type": "request-start"
    },
    {
      "content": "I'm having trouble completing that booking.",
      "type": "request-failed"
    }
  ],
  "server": {
    "credentialId": "${VAPI_TOOLS_CREDENTIAL_ID}",
    "timeoutSeconds": 10,
    "url": "${GRACE_TOOLS_URL}"
  },
  "type": "function"
}
