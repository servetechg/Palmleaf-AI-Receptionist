{
  "//": "Documents which n8n credentials must exist on the instance. NEVER commit real values.",
  "//dev": "Credential entries in workflow JSON reference __CRED__:<alias> placeholders (doc 19 §A2 point 1), resolved by deploy.ts via GET /api/v1/credentials at push time.",
  "required": [
    { "alias": "slack", "type": "slackApi", "dev": "PalmLeaf Slack (dev)", "prod": "PalmLeaf Slack (prod)" },
    { "alias": "twilio", "type": "twilioApi", "dev": "PalmLeaf Twilio (dev)", "prod": "PalmLeaf Twilio (prod)" },
    { "alias": "core-api", "type": "httpHeaderAuth", "dev": "PalmLeaf Core API (dev)", "prod": "PalmLeaf Core API (prod)" }
  ]
}
