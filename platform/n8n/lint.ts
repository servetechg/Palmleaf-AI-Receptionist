// Parses each workflow JSON in ./workflows and asserts the 15 structural rules from doc 09 §5 +
// doc 19 §A2 point 5 (env-prefix, credential placeholder, errorWorkflow placeholder, Wait-node
// durability, Slack raw-body/respond-node placement, etc). Runs on every PR.
//
// TODO(build step B5): implement once the first workflow (WF-12) exists.

export {};
