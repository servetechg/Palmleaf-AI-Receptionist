<!-- GENERATED by grace_platform.docs.gen_tools — DO NOT EDIT.
     Source of truth: src/grace_contracts/tools/registry.py and the generated tool JSON.
     Regenerate with: make docs -->

# `cancelAppointment`

**Kind:** function tool — a question Grace waits on
**Source model:** `grace_contracts.tools.write_tools.CancelAppointmentInput`
**Generated JSON:** `platform/vapi/tools/cancelAppointment.json`

## What it does, and when Grace calls it

Cancel an existing appointment Grace can see. The tool decides the cancellation fee — you never do, never waive, never negotiate. State the fee before confirming, and set feeAcknowledged only on a clear yes. If the caller disputes the fee, or the booking can't be found, hand over or take a message — never claim a cancellation you couldn't complete.

## Parameters

| Parameter | Type / allowed values | Required | Default | What it means |
|---|---|---|---|---|
| `bookingRef` | `string`, pattern `^[Bb][Kk]-[0-9A-Za-z]{4,8}$` | **yes** | — | Booking reference exactly as it was given to you, e.g. bk-3QR9. |
| `feeAcknowledged` | `boolean` | no | `false` | Set true only after stating the cancellation fee and hearing the caller accept it. |

Any parameter not listed above is **rejected**, not ignored — the model inventing a field must be a loud error, never silent.

## Settings

| Setting | Value | Why |
|---|---|---|
| Runs in background | no | Grace waits for the answer before speaking |
| Changes data | **yes** | Must be idempotent on `callId:toolCallId` (I3) |
| p95 target | 600 ms | A target for alerting, **not** a deadline — the deadline is `GRACE_TOOL_DEADLINE_MS` |
| Request timeout | 10 s | Above our own deadline, so our graceful sentence fires first |
| Retry | **none** | A retried write is a real duplicate — a second booking, a second text |
| Live Vapi ID | `9f3dc634-63ef-49a4-be51-c369a2bca637` | Vapi dashboard → **Tools** |

## What the caller hears

- **While it runs:** "Let me pull that up."
- **If it fails:** "I'm having trouble cancelling that appointment."

## Where to see it

Vapi dashboard → **Tools** → `cancelAppointment` (`9f3dc634-63ef-49a4-be51-c369a2bca637`).
It is attached to the assistant under **Assistants** → *Grace — PalmLeaf [dev]* → *Tools*.
